import CinematicRoom from '../../components/CinematicRoom';

/** Review before promotion: mobile composition plus approved shared cleanups. */
export default function MobileLayoutReview(){
  return <CinematicRoom coherentPhotos shelfReview mobileLayout />;
}
