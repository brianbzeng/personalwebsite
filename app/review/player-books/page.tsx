import PlayerBookReview from './review';

export const metadata = { title: 'Player & book review — Brian Zeng', robots: { index: false, follow: false } };
export default function ReviewPage() { return <PlayerBookReview />; }
