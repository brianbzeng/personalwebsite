import CinematicRoom from '../../components/CinematicRoom';

/** Staging only: corrected neighboring record shelf in all photo views. */
export default function PhotoConsistencyReview(){
  return <CinematicRoom coherentPhotos shelfReview />;
}
