import CinematicRoom from '../../components/CinematicRoom';

/** Retained review entry point for the now-approved full-width camera framing. */
export default function CameraFitReview() {
  return <CinematicRoom />;
}
